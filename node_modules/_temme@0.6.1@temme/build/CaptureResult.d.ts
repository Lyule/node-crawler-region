import { Filter } from './interfaces';
import { FilterFnMap } from './filters';
export declare class CaptureResult {
    private filterFnMap;
    private result;
    private failed;
    constructor(filterFnMap: FilterFnMap);
    setFailed(): void;
    isFailed(): boolean;
    add(key: string, value: any, filterList?: Filter[]): void;
    forceAdd(key: string, value: any, filterList?: Filter[]): void;
    merge(other: CaptureResult): void;
    mergeWithFailPropagation(other: CaptureResult): void;
    get(): any;
    private doMerge(other);
    private applyFilter(value, filter);
    private applyFilterList(initValue, filterList);
}
